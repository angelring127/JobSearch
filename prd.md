# JobMap – Product Requirements Document (PRD)

## 1. 제품 개요

**JobMap**은 캐나다 구인정보 게시판(JPCanada 등)에 등록된 일자리 정보를 크롤링·정제하여 **지도 기반으로 시각화**하는 웹 서비스입니다. 사용자는 자신의 위치 주변 또는 관심 지역의 일자리를 직관적으로 탐색할 수 있습니다.

---

## 2. 문제 정의

| 문제             | 세부 설명                                                                            |
| ---------------- | ------------------------------------------------------------------------------------ |
| 🔍 검색의 불편함 | 기존 사이트(JPCanada)는 지역별 게시판(BBS)로 나뉘어 있고, 위치 기반 검색 기능이 없음 |
| 🧭 탐색의 비효율 | 사용자는 일일이 게시판을 눌러야 하며, 직관적인 공간 감각 없이 텍스트로만 탐색        |
| 📉 중복 정보     | 동일 일자리가 여러 BBS에 중복 게시되는 경우, 사용자가 혼동                           |

---

## 3. 목표

- 지도를 중심으로 **시각적이고 직관적인 구직 탐색 경험 제공**
- **위치 기반 검색 및 반경 필터**로 접근성 향상
- **크롤링 자동화 + 데이터 정제**로 게시글을 통합 관리
- 사용자에게 **직관적인 리스트-맵 연동 UI** 제공

---

## 4. 주요 사용자 (User Persona)

| 구분                   | 설명                    | 주요 니즈                                            |
| ---------------------- | ----------------------- | ---------------------------------------------------- |
| 구직자 (일반 사용자)   | 캐나다 현지 혹은 워홀러 | 지역별 아르바이트/풀타임 일자리 탐색, 지도 기반 검색 |
| 소규모 사업자 (고용주) | 구인 게시자             | 내 점포/카페 주변 인력 노출을 높이고 싶음            |
| 여행자/학생            | 단기 일자리 탐색자      | 숙소 근처에서 쉽게 일자리를 찾고 싶음                |

---

## 5. 기능 요약

### (1) 데이터 수집 / 정제

- JPCanada BBS별 크롤링 (밴쿠버, 빅토리아, 토론토, 휘슬러, 기타)
- `msgid` 커서 기반 크롤링 → 증분 수집
- 원문은 저장하지 않고, **메타데이터(URL, 시급, 위치명 등)**만 저장

### (2) 데이터베이스 구조 (요약)

| 테이블        | 설명                                                                                                    |
| ------------- | ------------------------------------------------------------------------------------------------------- |
| `regions`     | 도시별 BBS 메타데이터 (id, city, bbs, url, 중심좌표)                                                    |
| `job_sources` | 게시글 메타데이터 (id, msgid, source_url, lat, lng, wage, category, confidence, region_hint, posted_at) |
| `crawl_log`   | 크롤링 이력 관리 (bbs_id, last_seen_msgid, updated_at)                                                  |

### (3) API 주요 기능

| 엔드포인트               | 설명                                   |
| ------------------------ | -------------------------------------- |
| `GET /api/jobs/viewport` | 지도 뷰포트 내 일자리 검색 (BBox 기준) |
| `GET /api/jobs/nearby`   | 반경(Radius) 기반 검색                 |
| `GET /api/jobs/clusters` | 줌 레벨별 클러스터링 API               |
| `POST /internal/ingest`  | 크롤러가 새 데이터 삽입 시 사용        |

### (4) API 응답 형식

**성공 응답 (200 OK):**

```json
{
  "success": true,
  "data": [...],
  "meta": {
    "count": 42,
    "total": 42
  }
}
```

**에러 응답 (4xx/5xx):**

```json
{
  "success": false,
  "error": {
    "code": "INVALID_BBOX",
    "message": "Invalid bounding box parameters",
    "details": {}
  }
}
```

**주요 에러 코드:**

- `INVALID_BBOX`: 잘못된 경계 박스 파라미터
- `INVALID_COORDINATES`: 잘못된 좌표값
- `RATE_LIMIT_EXCEEDED`: 요청 한도 초과
- `INTERNAL_ERROR`: 서버 내부 오류
- `NOT_FOUND`: 리소스를 찾을 수 없음

---

## 6. UX 흐름

1️⃣ 사용자가 JobMap 페이지 진입  
2️⃣ 기본적으로 밴쿠버 지역 중심으로 지도 렌더링  
3️⃣ 지도 이동 또는 확대 시, 뷰포트 기준으로 구인정보 API 호출  
4️⃣ 좌측 패널 리스트 및 지도 핀 동기화  
5️⃣ 핀 클릭 → 하단 상세 팝업 / "View on JPCanada" 링크  
6️⃣ 시급/직종 필터 적용 가능

---

## 7. 주요 UI 구성

| 구역           | 설명                                              |
| -------------- | ------------------------------------------------- |
| 🗺 지도 영역    | Mapbox/MapLibre 기반 지도, 핀 및 클러스터 표시    |
| 📋 리스트 패널 | 현재 지도 내 구인정보 목록, 클릭 시 핀 하이라이트 |
| 🧭 필터 바     | 시급, 직종, 거리(반경) 필터                       |
| 📍 팝업        | 선택된 구인정보 상세 요약 + JPCanada 원문 링크    |

---

## 8. 시스템 구성도

```
[Crawler (Python)] → [Supabase/PostgreSQL + PostGIS] ←→ [FastAPI Backend] ←→ [Next.js Frontend]
```

- 크롤러: Python Requests + BeautifulSoup (msgid 커서 방식)
- 백엔드: FastAPI (CRUD, 뷰포트/반경 쿼리)
- 프론트엔드: Next.js 15 + Tailwind + Mapbox/MapLibre
- DB: Supabase (PostGIS 확장)

---

## 9. 크롤링 정책 요약

| 항목        | 내용                          |
| ----------- | ----------------------------- |
| 요청 간격   | 2.5초 이상                    |
| 신규 판단   | `msgid > last_seen_max_msgid` |
| 데이터 보존 | URL/좌표/시급 등 메타만 저장  |
| 중복 방지   | 전화번호/이메일 해시 기반     |
| 빈도 제어   | 도시별 15~30분 간격 크론      |

---

## 10. 성공 지표 (KPI)

- 📈 사용자 유지율 (30일 재방문률 > 40%)
- 📊 평균 탐색 시간 1분 미만 (검색→결정)
- 🧭 지도 기반 탐색 사용률 > 80%
- 💬 게시글 중복 비율 < 5%

---

## 11. 향후 확장

- Indeed, Kijiji 등 외부 구인사이트 추가 통합
- 구직자 북마크 기능
- 이메일 알림: “이 지역에 새 일자리 등록됨!”
- 다국어 지원 (KR / JP / EN)

---

## 12. MVP 범위

✅ BBS 크롤링 (5개 지역)  
✅ FastAPI `/api/jobs/viewport`  
✅ 지도 + 리스트 UI  
✅ 필터: 시급/거리  
✅ 'View on JPCanada' 링크

---

## 13. 에러 처리 정책

### 사용자 피드백 가이드라인

| 상황            | 사용자 피드백                                              | 액션                                 |
| --------------- | ---------------------------------------------------------- | ------------------------------------ |
| 네트워크 오류   | "연결에 실패했습니다. 다시 시도해주세요."                  | 재시도 버튼 제공                     |
| 빈 검색 결과    | "이 지역에서 일자리를 찾을 수 없습니다."                   | 검색 범위 확대 제안                  |
| API 오류 (4xx)  | "요청을 처리할 수 없습니다."                               | 필터/파라미터 재확인 안내            |
| 서버 오류 (5xx) | "일시적인 문제가 발생했습니다. 잠시 후 다시 시도해주세요." | 자동 재시도 (최대 3회)               |
| 지오코딩 실패   | "위치 정보를 확인할 수 없습니다."                          | 원문 링크 제공, confidence 낮게 표시 |

### 프론트엔드 에러 처리

- 모든 API 호출에 try-catch 적용
- 에러 발생 시 토스트 알림 표시
- 부분 실패 시 부분 결과 유지
- 로딩 상태 명확히 표시 (스켈레톤 UI)

---

## 14. 환경 변수 및 설정 관리

### 프론트엔드 환경 변수

- `NEXT_PUBLIC_API_URL`: 백엔드 API 기본 URL
- `NEXT_PUBLIC_MAPLIBRE_TOKEN`: MapLibre 스타일 토큰 (선택)

### 백엔드 환경 변수

- `DATABASE_URL`: Supabase PostgreSQL 연결 문자열
- `API_KEY`: `/internal/ingest` 엔드포인트 인증 키
- `NOMINATIM_URL`: OpenStreetMap Nominatim API URL
- `LOG_LEVEL`: 로깅 레벨 (DEBUG, INFO, WARNING, ERROR)

### 크롤러 환경 변수

- `API_BASE_URL`: 백엔드 API 기본 URL
- `API_KEY`: 인증 키
- `CRAWL_INTERVAL`: 크롤링 간격 (초)
- `USER_AGENT`: 크롤러 User-Agent 문자열

### 설정 파일 구조

- `.env.example`: 환경 변수 템플릿 파일
- 각 서비스별 `.env` 파일은 `.gitignore`에 포함
- 프로덕션 환경은 호스팅 플랫폼의 환경 변수 설정 사용

---

## 15. 테스트 전략 및 품질 보증

### 단위 테스트

- 백엔드: FastAPI 엔드포인트 단위 테스트 (pytest)
- 크롤러: 파싱 로직, 데이터 정제 로직 테스트
- 목표 커버리지: 70% 이상

### 통합 테스트

- API 엔드포인트 통합 테스트
- 데이터베이스 쿼리 검증
- 지오코딩 서비스 연동 테스트

### E2E 테스트 (선택)

- 프론트엔드 주요 사용자 흐름 테스트
- 지도 인터랙션 테스트

### 품질 보증 기준

- API 응답 시간: 평균 200ms 이하
- 지도 렌더링: 초기 로드 2초 이하
- 크롤러 안정성: 에러율 5% 이하
- 데이터 정확도: 지오코딩 confidence 평균 0.7 이상

---

이 PRD는 JobMap의 **1차 MVP 개발 범위와 목표, UX 플로우, API 및 수집 구조를 정의**합니다.
